# /src/logic/midi_tools.py


def run_basic_pitch(audio_path):
    """
    Placeholder for BasicPitch model execution.
    Should return a list of MIDI note events or save a MIDI file.
    You can implement real logic using the official BasicPitch Python API.

    Example real return structure (future):
    {
        "midi_path": "generated/song.mid",
        "notes": [
            {"pitch": 60, "start": 0.5, "end": 1.0, "velocity": 80},
            ...
        ]
    }
    """
    return {"midi_path": "generated/example.mid", "notes": []}
